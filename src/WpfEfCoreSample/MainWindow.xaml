<Window x:Class="WpfEfCoreSample.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfEfCoreSample"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>

<StackPanel Margin="20">
            <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                <TextBlock Text="名前：" Width="50" VerticalAlignment="Center"/>
                <TextBox x:Name="NameTextBox" Width="200" />
            </StackPanel>
            <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                <TextBlock Text="年齢：" Width="50" VerticalAlignment="Center"/>
                <TextBox x:Name="AgeTextBox" Width="100" />
            </StackPanel>
            <Button x:Name="AddButton" Width="100" Content="追加" Margin="0,0,0,10" Click="AddButton_Click"/>
            <ListBox x:Name="PeopleListBox" Width="300" Height="300"/>
        </StackPanel>
    </Grid>
</Window>
